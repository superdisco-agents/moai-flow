{
  "_preset_metadata": {
    "name": "Manual - Local Git Only",
    "mode": "manual",
    "environment": "local",
    "description": "Local Git workflow for developers who want complete Git control. No GitHub integration. Branch creation is manual, but TDD and documentation commits are automated.",
    "use_case": "Individual developers, local-only development, full Git control required",
    "version": "1.0.0",
    "created_at": "2025-11-23"
  },

  "git_strategy": {
    "mode": "manual",
    "environment": "local",
    "github_integration": false,
    "description": "Complete local Git control. No GitHub automation.",

    "branch_creation": {
      "prompt_always": true,
      "auto_enabled": false,
      "description": "Manual mode: Branch creation is manual (auto_enabled ignored). User chooses on every SPEC."
    },

    "automation": {
      "auto_branch": false,
      "auto_commit": true,
      "auto_pr": false,
      "auto_push": false,
      "notes": "Branch creation is manual (user controls). TDD and documentation commits are automatic. No PR creation. Push requires manual confirmation."
    },

    "hooks": {
      "pre_commit": "enforce",
      "pre_push": "warn",
      "commit_msg": "warn",
      "notes": "pre_commit: enforce → formatting violations block commit (ruff, black must pass). pre_push: warn → show warnings but allow push. commit_msg: warn → suggest but don't block"
    },

    "commit_style": {
      "format": "conventional",
      "scope_required": false,
      "tdd_prefixes": {
        "red": "🔴 RED",
        "green": "🟢 GREEN",
        "refactor": "♻️ REFACTOR",
        "docs": "📚 DOCS"
      },
      "notes": "TDD cycle uses colored emoji prefixes. Example: '🔴 RED: Add user authentication tests' → '🟢 GREEN: Implement basic auth' → '♻️ REFACTOR: Optimize auth logic'"
    },

    "branch_strategy": {
      "type": "manual",
      "main_branch": "main",
      "protected_branches": ["main"],
      "default_branch_for_features": null,
      "notes": "User creates and manages branches manually. No automatic branch creation from SPEC"
    }
  },

  "developer_workflow": {
    "step_1_plan": {
      "command": "/moai:1-plan \"Description\"",
      "action": "Creates SPEC file",
      "auto_branch": false,
      "notes": "Generates SPEC-XXX in .moai/specs/ but does NOT create branch"
    },
    "step_2_branch_manual": {
      "instruction": "User manually creates feature branch",
      "example": "git checkout -b feature/SPEC-001-description",
      "notes": "User controls branch creation. Feature branches are optional but recommended"
    },
    "step_3_run": {
      "command": "/moai:2-run SPEC-001",
      "action": "Execute TDD cycle",
      "auto_commit": true,
      "commits": [
        "🔴 RED: [test description]",
        "🟢 GREEN: [implementation description]",
        "♻️ REFACTOR: [optimization description]"
      ],
      "formatting": "enforce",
      "notes": "Automatic commits for RED-GREEN-REFACTOR phases. Formatting (ruff, black) is enforced. Commit failures blocked if formatting fails."
    },
    "step_4_sync": {
      "command": "/moai:3-sync SPEC-001",
      "action": "Generate documentation",
      "auto_commit": true,
      "commit": "📚 DOCS: [API/architecture documentation]",
      "pr_creation": false,
      "notes": "Automatic documentation commit. No PR created (local workflow)"
    },
    "step_5_review": {
      "instruction": "Local review of commits",
      "command": "git log --oneline -n 5",
      "notes": "Review all automatic commits before pushing"
    },
    "step_6_push_manual": {
      "instruction": "User manually pushes to local/remote",
      "commands": [
        "git push origin feature/SPEC-001",
        "# OR for main branch (if direct commit)"
      ],
      "notes": "User controls push timing and target. Pre-push hooks show warnings but don't block."
    }
  },

  "quality_gates": {
    "test_coverage": {
      "target": 85,
      "enforce": true,
      "block_merge": false,
      "notes": "Target 85% coverage. Enforced during /moai:2-run. Warnings shown but don't block in manual mode."
    },
    "formatting": {
      "ruff": {
        "enabled": true,
        "enforce": true,
        "block_commit": true
      },
      "black": {
        "enabled": true,
        "enforce": true,
        "block_commit": true
      },
      "notes": "Formatting violations block commit (pre_commit: enforce). User must fix formatting before commit succeeds."
    },
    "security": {
      "enabled": true,
      "level": "basic",
      "notes": "Basic security checks. User can override with --no-verify (not recommended)"
    }
  },

  "push_strategy": {
    "auto_push": false,
    "push_prompt": "Before pushing, review: git diff origin/main...HEAD",
    "notes": "No automatic push. User reviews and manually pushes. Pre-push hooks validate against secret patterns (blocking)."
  },

  "notes": {
    "ideal_for": "Individual developers who want complete Git control without GitHub automation",
    "workflow_time": "Self-paced (no automation pressure)",
    "collaboration": "Not suitable for team collaboration (no GitHub integration)",
    "best_practices": [
      "1. Create feature branches for each SPEC",
      "2. Review auto-commits before pushing",
      "3. Test locally before push",
      "4. Use descriptive branch names (feature/SPEC-XXX-description)"
    ]
  }
}
