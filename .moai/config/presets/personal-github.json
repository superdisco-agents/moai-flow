{
  "_preset_metadata": {
    "name": "Personal - GitHub Individual Project",
    "mode": "personal",
    "environment": "github",
    "description": "Automated GitHub Flow for individual developers. All automation enabled (branches, commits, push) except PR which is suggested to user.",
    "use_case": "Individual GitHub projects, rapid development, autopilot mode",
    "version": "1.0.0",
    "created_at": "2025-11-23"
  },

  "git_strategy": {
    "mode": "personal",
    "environment": "github",
    "github_integration": true,
    "description": "Full GitHub Flow automation for individual projects.",

    "branch_creation": {
      "prompt_always": true,
      "auto_enabled": false,
      "description": "Personal mode: Default is manual (no branch creation). prompt_always=false + auto_enabled=true enables full automation."
    },

    "automation": {
      "auto_branch": true,
      "auto_commit": true,
      "auto_pr": false,
      "auto_push": true,
      "notes": "Branch: 🤖 auto. Commit: 🤖 auto. PR: 💡 suggested (user decides). Push: 🤖 auto"
    },

    "hooks": {
      "pre_commit": "enforce",
      "pre_push": "enforce",
      "commit_msg": "warn",
      "notes": "pre_commit: enforce → formatting violations block commit. pre_push: enforce → security checks block push. commit_msg: warn → suggestions for conventional format"
    },

    "commit_style": {
      "format": "conventional",
      "scope_required": false,
      "tdd_prefixes": {
        "red": "🔴 RED",
        "green": "🟢 GREEN",
        "refactor": "♻️ REFACTOR",
        "docs": "📚 DOCS"
      },
      "notes": "TDD cycle uses colored emoji prefixes for clear visibility in commit history"
    },

    "branch_strategy": {
      "type": "github-flow",
      "workflow_name": "GitHub Flow",
      "main_branch": "main",
      "protected_branches": ["main"],
      "feature_branch_pattern": "feature/SPEC-{ID}-{description}",
      "auto_create": true,
      "auto_delete_after_merge": true,
      "notes": "Automatic feature branch creation from SPEC. Auto-cleanup after merge."
    }
  },

  "developer_workflow": {
    "step_1_plan": {
      "command": "/moai:1-plan \"Description\"",
      "action": "Creates SPEC file + branch",
      "auto_branch": true,
      "branch_created": "feature/SPEC-001-description",
      "notes": "SPEC file created in .moai/specs/SPEC-001/. Feature branch automatically created and checked out."
    },
    "step_2_ready": {
      "status": "Ready to code",
      "branch": "feature/SPEC-001-description (auto-created)",
      "notes": "No manual branch creation needed. Alfred handles everything."
    },
    "step_3_run": {
      "command": "/moai:2-run SPEC-001",
      "action": "Execute TDD cycle",
      "auto_commit": true,
      "commits": [
        "🔴 RED: [test description]",
        "🟢 GREEN: [implementation description]",
        "♻️ REFACTOR: [optimization description]"
      ],
      "formatting": "enforce",
      "notes": "Automatic commits pushed to feature branch. Formatting enforced."
    },
    "step_4_sync": {
      "command": "/moai:3-sync SPEC-001",
      "action": "Generate documentation",
      "auto_commit": true,
      "commit": "📚 DOCS: [API/architecture documentation]",
      "pr_creation": "suggested",
      "notes": "Documentation auto-committed. Alfred suggests creating PR to main."
    },
    "step_5_review": {
      "instruction": "Review commits and decide on PR",
      "command": "git log origin/main..HEAD --oneline",
      "user_choice": "Create PR or merge directly",
      "notes": "User reviews commits on feature branch. Decides whether to create PR."
    },
    "step_6_merge": {
      "option_a": {
        "choice": "Create PR (Recommended)",
        "action": "GitHub PR to main",
        "pr_type": "standard",
        "branch_cleanup": "auto (after merge)"
      },
      "option_b": {
        "choice": "Merge directly",
        "action": "git merge feature/SPEC-001 && git push",
        "notes": "Skip PR for faster workflow"
      }
    }
  },

  "quality_gates": {
    "test_coverage": {
      "target": 85,
      "enforce": true,
      "block_merge": false,
      "notes": "85% minimum coverage enforced during /moai:2-run. Warnings shown, not blocking."
    },
    "formatting": {
      "ruff": {
        "enabled": true,
        "enforce": true,
        "block_commit": true
      },
      "black": {
        "enabled": true,
        "enforce": true,
        "block_commit": true
      },
      "notes": "Formatting violations block commit at pre_commit hook."
    },
    "security": {
      "enabled": true,
      "level": "standard",
      "block_push": true,
      "notes": "Security scanning during pre_push. Secrets detection blocks push."
    }
  },

  "github_integration": {
    "auto_branch": true,
    "branch_creation_timing": "at /moai:1-plan",
    "branch_cleanup": "after merge",
    "auto_push": true,
    "push_timing": "after each commit during TDD phases",
    "pr_creation": "suggested (user decides)",
    "pr_type": "standard",
    "pr_auto_merge": false,
    "notes": "Full automation except PR which requires user decision. PR creation suggested, not automatic."
  },

  "workflow_comparison": {
    "vs_manual": "Personal adds: auto branch creation, auto push, GitHub integration",
    "vs_team": "Personal differs: no auto PR, no branch protection, no required reviews"
  },

  "notes": {
    "ideal_for": "Individual developers using GitHub for their projects. Want maximum automation without team management overhead.",
    "workflow_time": "Fast (autopilot mode). No waiting for reviews.",
    "collaboration": "Suitable for solo projects on GitHub",
    "automation_level": "High (85% automated)",
    "best_practices": [
      "1. Always review auto-commits on feature branch before PR",
      "2. Use descriptive SPEC titles",
      "3. Test locally before PR (optional, but recommended)",
      "4. Create PR for each SPEC to maintain history"
    ]
  }
}
